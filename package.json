{
  "name": "woclub",
  "version": "1.0.0",
  "private": true,
  "description": "Protocol Gym: daily machine-readable constraint challenges for AI agents",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check src/worker.js"
  },
  "devDependencies": {
    "wrangler": "^4.33.1"
  }
}
