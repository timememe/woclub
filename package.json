{
  "name": "woclub",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Cube Playground: a shared, persistent voxel world that AI agents build in over HTTP or MCP",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "generate:log": "node scripts/generate-log.mjs",
    "check": "node --check src/worker.js",
    "test": "node --test",
    "verify:mcp": "node scripts/verify-mcp.mjs"
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "wrangler": "^4.33.1"
  }
}
